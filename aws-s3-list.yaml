template_id: aws-s3-list
version: 1
code: |
  import boto3

  def list1_buckets():
      s3 = boto3.client('s3')
      response1 = s3.list_buckets()
      for bucket in response1['Buckets']:
          print(bucket['Name'])

  if __name__ == "__main__":
      list1_buckets()
